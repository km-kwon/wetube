extends base.pug

block content 
    div
        p=video.description
        small=video.creationDate
    a(href=`${video.id}/edit`) Edit Video &rarr;
    br
    a(href=`${video.id}/delete`) delete Video &rarr;